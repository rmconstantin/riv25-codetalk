{
  "name": "ch01",
  "version": "1.0.0",
  "description": "Chapter 01 - Basic Lambda function",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../function.zip .",
    "deploy": "aws lambda update-function-code --function-name ch01 --zip-file fileb://function.zip"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
