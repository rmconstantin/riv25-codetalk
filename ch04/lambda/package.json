{
  "name": "ch04",
  "version": "1.0.0",
  "description": "Chapter 04 - Money transfer with transactions",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../function.zip .",
    "deploy": "aws lambda update-function-code --function-name ch04 --zip-file fileb://function.zip"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@aws-sdk/dsql-signer": "^3.0.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
